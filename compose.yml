services:
  mirrord:
    image: ghcr.io/nemvince/mirrord:latest
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - /mirrors:/data
      - mirrord_db:/tmp/mirrord

volumes:
  mirrord_db:
