services:
  mirrord:
    image: ghcr.io/nemvince/mirrord:latest
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - /mirrors:/data
    restart: unless-stopped
